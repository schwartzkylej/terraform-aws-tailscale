name: Test

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read
  id-token: write
  issues: write
  pull-requests: write

jobs:
  detect-secrets:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout code
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332 # v4
        with:
          fetch-depth: 0

      - name: Scan for secrets
        id: trufflehog
        uses: trufflesecurity/trufflehog@048ec26c9261f70732d4403df2f45561c41c8161 # v3.80.5
        continue-on-error: true
        with:
          extra_args: --only-verified

      - name: Secrets scan results
        if: steps.trufflehog.outcome == 'failure'
        run: exit 1
